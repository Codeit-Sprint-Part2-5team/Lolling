import PlusIcon from '../../assets/images/PlusIcon.svg';
import * as S from './PlusButton.styled';

function PlusButton() {
  return <S.PlusButtonLayout src={PlusIcon} alt="더하기 이미지" />;
}

export default PlusButton;
